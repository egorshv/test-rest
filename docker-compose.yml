version: "3.9"

services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      API_KEY: secret-api-key
      DATABASE_URL: sqlite:////app/app.db
    volumes:
      - ./app.db:/app/app.db
